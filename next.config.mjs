/**
 * Configuracao do Next.js.
 * Por enquanto fica vazia (defaults). Conforme o app cresce, opcoes entram aqui.
 * O App Router ja vem ligado por padrao por usarmos a pasta src/app.
 */
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
